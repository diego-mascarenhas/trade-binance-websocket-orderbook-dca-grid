#!/usr/bin/env bash
# Default: execute · fib 1m · direction auto
#   ./obmicro-grid LDOUSDT
# Paper:
#   ./obmicro-grid LDOUSDT --dry-run
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 -u "$ROOT/orderbook_micro_grid.py" "$@"
